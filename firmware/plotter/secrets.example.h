#pragma once

// Optional: copy this file to `secrets.h` and fill in your Wi-Fi credentials
// to skip the WiFiManager captive portal. Useful when re-flashing repeatedly.
//
//   cp firmware/plotter/secrets.example.h firmware/plotter/secrets.h
//
// `secrets.h` is gitignored — your credentials never enter the repo.

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"
